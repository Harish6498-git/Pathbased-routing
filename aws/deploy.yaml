apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-aws
spec:
  replicas: 2
  selector: { matchLabels: { app: web-aws } }
  template:
    metadata: { labels: { app: web-aws } }
    spec:
      containers:
      - name: web-landing
        image: docker.io/harish0604/pathrouter-aws:v3
        ports: [{ containerPort: 80 }]
        resources:
          requests: { cpu: "50m", memory: "64Mi" }
          limits:   { cpu: "200m", memory: "256Mi" }
        readinessProbe: { httpGet: { path: "/", port: 80 }, initialDelaySeconds: 3 }
        livenessProbe:  { httpGet: { path: "/", port: 80 }, initialDelaySeconds: 5 }
