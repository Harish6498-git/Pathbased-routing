server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Fall back to index.html for any path (SPA-friendly)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
